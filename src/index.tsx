import React from 'react';
import ReactDOM from 'react-dom/client';
import TimelineBlock from './components/TimelineBlock';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<TimelineBlock />);